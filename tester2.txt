@(foo, int, (x|int) {
	:(x);
	<-(x);
});

@(bar, non, (func|fn, x|int) {
	func(x);
});

@(main, non, (args|str) {
	:("Hello World!");

	#(x, int);
	->(x);

	bar(foo, x);

	=(x, +(x, 1));
	++(x);
	--(x);

	?(==(x, 2), {
	
	}, {
	
	});

	~(>=(x, 0), {
		--(x);
	});
});